# Placeholder file for preview proxy setup
# Per-PR Nginx site configs are generated dynamically by
# scripts/preview/nginx/nginx_enable_site.sh and linked into
# /etc/nginx/sites-enabled as preview-<pr>.__INTERNAL_DNS_ZONE__.conf
# This file remains here to satisfy installer logic but contains no server block.
